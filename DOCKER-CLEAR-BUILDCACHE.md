# Build Cache

To auto-confirm the `docker builder prune` command, use the `-f` or `--force` flag:

```bash
docker builder prune -f
```

To remove **all** build cache (not just dangling), add `-a`:

```bash
docker builder prune -af
```

คำสั่ง docker system df ใช้สำหรับตรวจสอบการใช้พื้นที่ดิสก์ของ Docker รวมถึงอิมเมจ (Images) คอนเทนเนอร์ (Containers) วอลลุม (Volumes) และ Build Cache (แคชที่ระบบใช้บันทึกเลเยอร์ระหว่างสร้างอิมเมจ เพื่อช่วยให้การ Build ครั้งต่อไปเร็วขึ้น)
## 📊 วิธีเช็กพื้นที่การใช้งาน Build Cache
หากต้องการดูสรุปพื้นที่ดิสก์ทั้งหมดของ Docker ให้ใช้คำสั่ง:

```sh
docker system df
```
ระบบจะแสดงผลลัพธ์รวมถึงบรรทัด Build Cache ที่บอกขนาดพื้นที่ทั้งหมดและพื้นที่ที่สามารถดึงคืนได้ (Reclaimable):

```
TYPE            TOTAL     ACTIVE    SIZE      RECLAIMABLE
Images          5         2         1.45GB    840MB (57%)
Containers      3         1         120.5kB   40.2kB (33%)
Local Volumes   1         1         512MB     0B (0%)
Build Cache     42        0         4.12GB    4.12GB (100%)
```

## 🔍 วิธีดูรายละเอียดเจาะลึก
หากต้องการดูว่าไฟล์แคชตัวไหนที่กำลังกินพื้นที่อยู่ ให้เพิ่มแฟลグ -v (Verbose):

```sh
docker system df -v
```

จากนั้นให้เลื่อนลงไปดูที่หัวข้อ Build Cache Items ระบบจะแสดง ID, ประเภทของแคช, ขนาดไฟล์ และเวลาที่เข้าถึงล่าสุด
------------------------------
## 🧹 วิธีล้างข้อมูล Build Cache
หากบิลด์แคชมีขนาดใหญ่เกินไป คุณสามารถลบออกได้ด้วยคำสั่งต่อไปนี้:
## 1. ลบเฉพาะแคชที่ไม่ได้ใช้งาน (ปลอดภัย)
คำสั่งนี้จะลบเฉพาะแคชที่ตกค้าง แต่จะเก็บแคชหลักที่ยังต้องใช้อยู่ ทำให้การบิลด์รอบหน้าไม่ช้าลง:

```
docker builder prune
```

## 2. ลบแคชทั้งหมดแบบเกลี้ยงตับ
หากต้องการลบประวัติการบิลด์และแคชทั้งหมด (รวมถึงแคชของโปรเจกต์ที่เพิ่งบิลด์ไปไม่นานด้วย) ให้ใช้แฟล็ก -a:

```
docker builder prune -a
```

## 3. ล้างระบบ Docker ทั้งหมด
หากต้องการลบคอนเทนเนอร์ที่หยุดทำงาน, อิมเมจที่ตกค้าง และบิลด์แคชไปพร้อมกันในทีเดียว:

```sh
docker system prune
```

(หมายเหตุ: หากต้องการลบอิมเมจทั้งหมดที่ไม่ได้ใช้และบิลด์แคชทั้งหมดแบบเกลี้ยงระบบ ให้เพิ่มแฟล็กเป็น docker system prune -a)
------------------------------
## 🛡️ วิธีตั้งค่าจำกัดขนาดแคชอัตโนมัติ (ขั้นสูง)
หากคุณเจอปัญหาดิสก์เต็มจากการบิลด์บ่อยๆ คุณสามารถตั้งค่าให้ Docker เปิดระบบ Garbage Collection (GC) เพื่อจำกัดขนาดแคชสูงสุดแบบอัตโนมัติได้
แก้ไขไฟล์คอนฟิกของ Docker daemon (ปกติอยู่ที่ /etc/docker/daemon.json สำหรับ Linux) แล้วเพิ่มโค้ดนี้ลงไป:


```json
{
  "builder": {
    "gc": {
      "enabled": true,
      "defaultKeepStorage": "10GB"
    }
  }
}
```

(เมื่อเซฟไฟล์แล้ว ให้ทำการ Restart บริการ Docker เพื่อให้ระบบเริ่มจำกัดพื้นที่แคชไม่ให้เกิน 10GB)
